# CLAUDE.md — agent entry point

You are working in a repo that follows the colab-handbook conventions, or in the
handbook itself. This file is the operational distillation for agents; the
normative rules live in [`CONVENTIONS.md`](CONVENTIONS.md). If this file and
that one ever disagree, `CONVENTIONS.md` wins — and report the discrepancy.

## Before touching anything

1. Read the repo's `.github/project.yml`. It tells you `tier`, `trunk`,
   `production`, `deploy`, `stack`, and possibly toolchain pins.
   - **File missing?** Treat the repo as Tier B with trunk `main`, say so in
     your report, and propose adding the file. Do not invent a tier.
2. `gh issue list --label in-progress` — anything listed is someone else's.
   Do not take it.
3. Claim your issue **before** starting, not when finishing:
   `gh issue edit <N> --add-assignee @me --add-label in-progress`

## Hard rules — each one exists because we measured the failure

- **Never create a branch named `trunk`.** "Trunk" is a role: the branch
  sessions merge into. It is `main` (Tier B) or `dev` (Tier A). Read
  `project.yml` to learn which.
- **Never create a `dev` branch in a Tier B repo** — not even "to be ready".
  A release branch nothing consumes decays into noise (we have one 76 commits
  stale to prove it). Tier promotion is a deliberate checklist
  ([§9](CONVENTIONS.md#9-adopting-this)), not a side effect.
- **An imminent launch does not make a repo Tier A.** The test is: does a
  deploy target exist *today*?
- Branch names: `^(feat|fix|docs|chore|refactor|test|perf)/[a-z0-9._-]+$`,
  ending in the issue number(s): `feat/onboard-redesign-23`, or for a group
  `fix/import-fixes-115-114-113`. Existing branches that violate this are
  grandfathered — **do not rename them**; some are live worktrees.
- **Branch off trunk, never off another feature branch.**
- Conventional Commits (`feat:` `fix:` `docs:` `chore:` `refactor:` `test:`
  `perf:`). Release notes are generated by grouping on these prefixes — an
  unprefixed commit is invisible in the changelog.

## Merging to trunk

1. **Check CI is alive and green first**: `gh run list --branch <trunk> -L 1`.
   A "failure" that never started (billing, runner outage) still means stop —
   we once merged for 12 hours into repos whose CI was silently dead.
2. Squash-merge, one commit per unit of work.
3. **The merge message must say `Closes #N`** — one per issue in the group.
   A bare `(#N)` does not auto-close, and we measured 26/30 issues sitting
   open with their code long since merged because of exactly that.
4. Release your claim: `gh issue edit <N> --remove-label in-progress`.
   Do this even if you didn't finish — a stale claim silently blocks others.

## Releases — Tier A only, and not yours to perform

Promotion (`dev` → `main`, `--no-ff`, never squash) and tagging are performed
by the human operator. **Your job ends with the trunk merge prepared and the
Issue updated.** Prepare a release; do not perform one. If you believe a
release is overdue (e.g. a production bug fix is merged but unreleased), say
so explicitly in your report — that situation has bitten us in payroll.

**One graduated exception — the trunk merge itself.** If the repo's
`project.yml` says `autonomy: auto-trunk`, you may complete Phase B through
**`colab ship` and no other path** — it checks the preconditions (CI alive and
green, no new migrations, no hand-code conflicts) and refuses when any fail;
a refusal means a human finishes, not that you improvise around it. Raw
`git push` to the trunk is blocked by hook regardless. This exception never
extends to promotion, tags, or anything that deploys — those are human on
every repo, with no field that can say otherwise.

## Worktrees — optional

Claiming and branching work fine without a worktree. If the machine has the
`colab` CLI (`tools/`), prefer it:

```sh
colab claim 115 114 113 --worktree import-fixes-115-114-113
colab worktree new fix/import-fixes-115-114-113 --issues 115,114,113 --ports 2
colab worktree rm <name>        # releases all its claims, frees its ports
```

- Ports listed in any repo's `project.yml` `ports:` are reserved for that
  repo's trunk dev server — never use them for a worktree, even if the trunk
  server is currently down.
- Machine-specific setup (DB clones, dependency symlinks) lives in the repo's
  `.colab/hooks/`, not in your improvisation. If hooks exist, they run
  automatically; do not replicate their work by hand.
- **Tear down when done.** Finished-but-not-removed worktrees are the single
  most-skipped step we measured (8 of 9 sessions, 2.9 GB). If you cannot
  remove it (uncommitted work, unsure), say so in your report instead of
  silently leaving it.

## The Issue is the memory

Long-running work: comment progress onto the Issue. Anyone — human or agent —
resuming the feature should get full context from `gh issue view N` without
re-reading the codebase. When you learn something non-obvious (a gotcha, a
decision, a dead end), distill it onto the Issue before you stop.

## When in doubt

Prefer reporting a conflict over resolving it silently: a `project.yml` that
contradicts reality, toolchain versions that disagree between files, a claim
that looks stale, CI that is red for reasons unrelated to your change. These
are findings — surface them. The worst outcome is not a broken build; it is a
repo whose documentation lies.
